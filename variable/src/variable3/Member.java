package variable3;

public class Member {
     int memNo;
     String name;
     String number;
     String className;
     
     public void showMemberInfo() {
    	 System.out.println("회원번호는" + memNo + " " +  "이름은"+ name + " " +  "전화번호는" + number + " " +  "강좌명은"+ className);
     }
}
